public class  TesterOfBrackets {
    String brackets;
    TesterOfBrackets(String inputBrackets) {
        this.brackets = inputBrackets;
    }
    public void tesBrackets(String str) {
        String[] array =  new String[0]
        for (String element: array
             ) {
            if (array.lenght() == 0 && ")]".contains(element)) {
                return false;
            }
            else {

            }
        }
    }
}